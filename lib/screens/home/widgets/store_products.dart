import 'package:flutter/material.dart';

class store_product extends StatelessWidget {
  const store_product({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}